# Dev-only database credentials. In production, supply these from a real secret store
# (sealed-secrets, External Secrets, Vault, a cloud secret manager) instead of committing them.
apiVersion: v1
kind: Secret
metadata:
  name: ams-db
type: Opaque
stringData:
  POSTGRES_USER: "test"
  POSTGRES_PASSWORD: "postgres"
