apiVersion: v1
kind: Service
metadata:
  name: firerescue-service
  labels:
    app: firerescue-service
spec:
  selector:
    app: firerescue-service
  ports:
    - name: http
      port: 38089
      targetPort: 38089
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: firerescue-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: firerescue-service
  template:
    metadata:
      labels:
        app: firerescue-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "38089"
    spec:
      containers:
        - name: firerescue-service
          image: ams/firerescue-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 38089
          envFrom:
            - configMapRef:
                name: ams-config
          env:
            - { name: SERVER_PORT, value: "38089" }
            - { name: POSTGRES_URL, value: "jdbc:postgresql://postgres:5432/ams_firerescue" }
            - name: POSTGRES_USER
              valueFrom: { secretKeyRef: { name: ams-db, key: POSTGRES_USER } }
            - name: POSTGRES_PASSWORD
              valueFrom: { secretKeyRef: { name: ams-db, key: POSTGRES_PASSWORD } }
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 38089
            initialDelaySeconds: 25
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 38089
            initialDelaySeconds: 45
            periodSeconds: 15
          resources:
            requests:
              cpu: "200m"
              memory: "512Mi"
            limits:
              memory: "1Gi"
